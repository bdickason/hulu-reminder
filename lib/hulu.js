// Generated by CoffeeScript 1.4.0

/* Handle integration with Hulu API
*/


(function() {
  var Hulu;

  exports.Hulu = Hulu = (function() {

    function Hulu(cfg) {
      this.cfg = cfg;
    }

    Hulu.prototype.getShows = function(callback) {
      var id, name, order, shows, sort, url, url_base;
      url_base = "http://www.hulu.com/api/2.0/videos.json?video_type[]=episodes";
      shows = {
        "New Girl": 7529
      };
      sort = "original_premiere_date";
      order = "DESC";
      url = url_base;
      console.log(shows);
      for (name in shows) {
        id = shows[name];
        url += "&show_id[]=" + id;
        console.log(url);
      }
      url += "&sort=" + sort + "&order=" + order;
      return callback(data);
    };

    return Hulu;

  })();

}).call(this);
